const imagenes = document.querySelector('.card');
console.log(imagenes);

const todasimagenes = document.querySelectorAll('.card');
console.log(todasimagenes);

// selectores especificos como en CSS

const info = document.querySelector ('.premium .info');
console.log(info);

const hospedaje = document.querySelector('section.hospedaje .card:nth-child(2)');
console.log(hospedaje);

// seleccionar formulario

const formulario = document.querySelector('.contenido-hero #formulario');
console.log(formulario);


// seleccionar elementos html

const navegacion = document.querySelector('nav');
console.log(navegacion);