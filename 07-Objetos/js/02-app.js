// Object literal
const producto = {
    nombre : "monitor",
    precio : 320,
    disponible : true,
 }
 

 // FORMAS DE ACCESO A VARIABLES

 console.log(producto.disponible);
 console.log(producto.precio);
 console.log(producto.nombre);

 console.log(producto['nombre']);