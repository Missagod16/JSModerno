let resultado ;

// PI 
resultado = Math.PI;

// Redondear al mas cercano ya sea mayor o menor

resultado = Math.round(2.9);
resultado = Math.round(2.3);

// redondear a arriba

resultado = Math.ceil(2.1);

//redondear a abajo
resultado = Math.floor(2.9);

//Raiz cuadrada

resultado = Math.sqrt(16);

// Absoluto

resultado = Math.abs(-200);

// potencia
resultado = Math.pow(3,2);

// Minimo 
resultado = Math.min(1,4,5,2,0);

// Maximo

resultado = Math.max(1,4,5,2,0);

// aleatorio

resultado = Math.random() * 20;

//Aleatorio dentro de un rango...

resultado = Math.floor(Math.random() * 30);


console.log(resultado);